---
transition: fade
auto-animate: true
---

# API07 — Server-Side Request Forgery (SSRF)
The server becomes a proxy, and attackers choose the destination

```ts
app.post('/fetch', async (req, res) => {
  const response = await axios.get(req.body.url);
  res.send(response.data);
});
```

<div class="mt-8 space-y-4 text-lg">
    <p> The API fetches data <strong>on behalf of the user</strong>. </p>
    <ul class="list-disc ml-6 space-y-3 opacity-90">
        <li>User controls the <span v-mark.orange="1">target URL</span></li>
        <li>No validation on destination</li>
        <li>Internal services become reachable</li>
    </ul>
</div>